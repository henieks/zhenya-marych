<!doctype html>
<html lang="en">
   <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>
         {% if title %}
            {{ title }} |
         {% endif %}Zhenya Marych</title>
      <link rel="stylesheet" href="{{ '/assets/fonts/inter.css' | url }}">
      <link rel="stylesheet" href="{{ '/styles.css' | url }}">
   </head>
   <body>
      <main>
         {{ content | safe }}
      </main>
   </body>
</html>